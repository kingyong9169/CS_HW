/*
 * p2-1.c
 *
 *  Created on: 2020. 4. 5.
 *      Author: kingyong
 */
/*
#include <stdio.h>

#define MAX_SIZE 100 //MAX_SIZE define

float sum(float[],int); //함수의 원형
float input[MAX_SIZE],answer; //배열과 배열의 합을 나타내는 변수 선언
int i;

void main(void)
{
	for(i=0;i<MAX_SIZE;i++) //배열의 합을 구하려는 반복문
		input[i]=i; //input[i]에 i대입
	printf("==========[김동용]  [2017038064]==========\n");
	answer=sum(input,MAX_SIZE); // answer에 input[]의 주소와 MAX_SIZE를 인자로 한 함수의 호출 및 반환 값 대입
	printf("THE sum is : %f\n",answer); //합계 출력
}
float sum(float list[],int n)
{
	int i;
	float tempsum=0;
	for(i=0;i<n;i++)
		tempsum +=list[i]; //tempsum에 모든 배열의 원소를 더함
	return tempsum; //tempsum를 반환함
}
*/


