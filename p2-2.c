/*
 * p2-2.c
 *
 *  Created on: 2020. 4. 5.
 *      Author: kingyong
 */
/*
#include <stdio.h>
void print1(int *ptr,int rows); //함수 원형 선언

int main()
{
	int one[]={0,1,2,3,4}; //정수형 배열 선언

	printf("one	=%p\n",one); //one배열의 주소 출력
	printf("&one	=%p\n",&one); //one배열의 배열포인터 출력
	printf("&one[0]	=%p\n",&one[0]); //one[0]의 주소 출력
	printf("\n");
	printf("==========[김동용]  [2017038064]==========\n");
	print1(&one[0],5); //one[0]의 주소와 배열의 인덱스로 5를 인자로 전달

	return 0;
}
void print1 (int *ptr, int rows)
{
	int i;
	printf("Address \t 	Contents\n");
	for(i=0;i<rows;i++)
		printf("%p \t	%5d\n",ptr+i,*(ptr+i)); //ptr[i]의 주소 및 ptr[i]의 값 출력
	printf("\n");
}
*/


